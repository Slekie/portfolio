{
  "name": "portfolio",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently --kill-others-on-fail --names \"client,server\" --prefix-colors \"cyan,magenta\" \"npm run dev --prefix client\" \"npm run dev --prefix backend\"",
    "build": "npm run build --prefix client",
    "client": "npm run dev --prefix client",
    "server": "npm run dev --prefix backend",
    "install:all": "npm install --prefix client && npm install --prefix backend"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
